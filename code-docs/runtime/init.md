# runtime/__init__.py

**Path:** `runtime/__init__.py`
**Purpose:** Empty package init file. Makes the `runtime/` directory importable as a Python package.

No code content -- exists solely to enable `from runtime.jb_xxx import ...` imports.
